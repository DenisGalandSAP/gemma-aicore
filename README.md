# gemma-aicore (GitOps)

SAP AI Core serving templates synced by the `gemma-proxy-app` application.
`aicore/serving-template.yaml` deploys a reverse proxy that forwards inference
to a self-hosted Ollama Gemma over ngrok.
